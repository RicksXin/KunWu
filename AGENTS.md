# 《昆吾禁地》Codex 项目入口

本文件适用于整个仓库。`CLAUDE.md` 是唯一完整的长期工程约定；Codex 开始任何任务前，
必须完整阅读根目录 `CLAUDE.md`，并按任务继续阅读相关策划、PRD、技术方案和 Demo 文档。
本文件只保留入口说明与最高风险红线，不复制完整命令、术语表或实现流程。

## 事实源入口

- 策划到实现严格单向派生：`Docs/0.1策划草案/` → `Docs/PRD/` → 实现与验收。
- `Docs/PRD/` 只落地 0.1 试玩版本；与 `Docs/Demo/` 的 D0/D1 范围、进度、编号和验收隔离。
- 技术实现以 `Docs/01_技术实现方案.md` 为准。
- D0/D1 Demo 的范围、进度和验收统一从 `Docs/Demo/` 进入。
- `Docs/08_开发进度与待办.md` 只维护 P3 以后正式项目阶段状态。
- 文档冲突时按 `CLAUDE.md` 中的事实源与冲突处理规则执行，不凭当前页面猜规则。

## 最高风险红线

- 禁止手写或脚本伪造 Cocos `.meta`；新增资源交给 Cocos Creator 导入并分配 UUID。
- 移动已有 Cocos Component 或资源时必须连同 `.meta` 一起移动，保留 UUID 和引用。
- 禁止用场景生成器整体覆盖正式 `Camp.scene`；Camp 的编辑器场景与 Prefab 是视觉事实源。
- 战斗数据流必须保持 `CombatCommand → 结算器 → CombatEvent → 表现层`。
- 七维内部字段名冻结：`strength magic technique speed constitution armor resistance`。
- 单个 TypeScript 文件不得超过 300 行；按职责拆分，不得通过压缩格式规避。
- UI 设计基准是 `375×817`，营地全景逻辑宽度是 `1050`；固定 HUD 不随全景横滑。
- 只使用 pnpm，不使用 npm 或 yarn。
- 保留工作区中与当前任务无关的用户或其他代理改动，不顺带重构。
- 所有验证默认由用户亲自操作。除非用户在当前任务明确授权，Codex 不运行检查、测试、
  数据/场景校验、构建、预览，也不代替用户进行 Cocos 或浏览器验收。

## 记忆维护

- 长期规则只在 `CLAUDE.md` 完整维护；本文件仅在入口或最高风险红线变化时同步。
- 产品规则、页面要求、Demo 进度和接口细节应写入各自事实源，不继续堆入记忆文件。
- Figma 设计与交付规范见 `CLAUDE.md` 的“Figma 设计稿规范”。
