# 《昆吾禁地》PRD 09：UI、交互与新手引导

版本：0.5
日期：2026-08-01
模块：UX
负责人：产品、UI/UX、客户端

## 1. 模块目标

在竖屏Web中清晰呈现资源、修士、地图和战斗信息。UI必须同时支持鼠标和触控，不依赖颜色传达唯一信息。

## 2. 技术边界

- 使用 Cocos UI。
- 不引入React。
- UI 设计稿基准视窗为375×817；这是玩家可见窗口，不是大厅全景内容宽度。
- `1080×1920` 仅是迁移中的旧 Cocos 工程坐标，不作为新 UI 的尺寸来源；`360×640`
  只可用于世界 Tile、角色等像素素材的内部参考，不参与 UI 布局与 UI 素材尺寸推导。
- 适配浏览器安全区和地址栏变化。
- PC 浏览器按375:817视窗居中展示，高度优先占满可用区域，宽度按比例计算，视窗外为纯黑留边。
- H5、手机和平板同样保持375:817；设备比例不一致时等比缩小并留边，不拉伸视窗。
- 改变视窗尺寸不得把大厅全景、地图或其他横向内容整体压缩进一屏；超出可视窗口的内容继续通过横滑、滚动或分页查看。

## 3. 页面清单

全局：

- 启动和加载。
- 主菜单/继续游戏。
- 设置。
- 许可证和Credits。

营地：

- 大厅。
- 营地人物列表（NPC List）。
- 七座成长建筑和入山入口。
- 竞技场、遗迹、圣迹、教会未开放场景锚点。
- 资源详情。

修士：

- 名册。
- 角色详情。
- 招募。
- 职业树。
- 技能策略。

物品：

- 仓库。
- 装备。
- 打造。
- 交易。

野外：

- 入山整备弹窗（含编队、装载和地图选择）。
- 方格地图。
- POI详情。
- 属性检定。
- 临时营地。
- 战斗。
- 结算。

任务：

- 主线。
- 支线。
- 对话和回看。

## 4. 通用交互

- 最小触控区域48×48dp。
- 高风险操作二次确认。
- 长按显示物品/属性说明。
- 鼠标悬停与触控长按提供同等信息。
- 返回键先关弹窗，再返回页面。
- 加载期间禁止重复点击。
- 所有异步操作显示处理中状态。

## 5. 视觉信息

修士灵根资质、装备品质和危险等级同时使用：

- 颜色。
- 文字。
- 边框形状。
- 图标。

不能只靠颜色区分。

关键提示：

- 灵粮不足。
- 负重80%。
- 无法安全返回。
- 属性检定失败后果。
- Boss危险。
- 死亡和还魂。
- 存档失败。

## 6. 地图UI

常驻：

- 坐标。
- 当前灵粮。
- 当前/最大负重。
- 探查范围。
- 当前任务。
- 返回营地。

点击格子：

- 已知可达格显示路径和消耗。
- 未知格不显示内部对象。
- 固定POI显示类型、是否完成。
- 条件出口显示缺少条件。

## 6.1 大厅 UI

- 大厅的页面结构、建筑位置、横滑规则和未开放范围以 PRD-01 为唯一事实源。
- 顶部头像、五种生产资源和主线提示固定，不随营地全景横滑。
- 中央营地全景支持触控与鼠标横向拖动，默认定位城堡/入山所在的中部区域。
- 底部左侧固定显示设置、成就、排行榜、邮件、日常进度；右侧显示独立于顶部灵晶的灵石余额。
- 成就、排行榜、邮件、日常进度暂不实现，必须展示禁用或明确未开放状态。
- 天符玉和全部付费入口隐藏，不保留可见占位。
- 横滑位移超过 PRD-01 定义的阈值后取消建筑点击，避免拖动误触。

## 6.2 入山整备、编队与地图选择

入山整备不是独立页面。玩家点击大厅传送阵后，在原大厅上方打开模态层；全屏暗色遮罩
仍按 `375×817` 覆盖，但主面板使用居中的紧凑 `359×570` 逻辑尺寸，不铺满屏幕。顶部与
底部大厅 HUD 被遮罩但不销毁。由上到下固定为：

1. 4 个横向修士槽位：空位为人物黑影与“人选未定”；已上阵卡使用对应静态立绘，
   并直接压在立绘上显示当前灵息、姓名、职业·等级和境界文字，不增加信息底板；灵息
   只显示当前值，不显示 `/100`。修士立绘在卡框内水平居中，灵根资质使用独立边框。
2. 操作栏：编辑队伍、队伍栏、调息。新档队伍栏仅开放 1 队；锁定队伍显示锁定
   标识与灵石费用。调息当前禁用并标注暂未开放。
3. 当前/最大负重 `x/y`。
4. 灵粮、开山镐、探灵镜三条装载行：显示已携带/当前拥有、单件重量和 `- / +`；触控区
   不小于 `48×48dp`，增减后立即刷新负重并保存。
5. 历练、启程、离开三个按钮。历练当前禁用；启程打开地图选择面板；离开无损关闭。

点击“编辑队伍”打开第二层选人面板。面板纵向滚动展示全部已拥有修士，每行包含头像、
境界、灵根资质、姓名、职业、等级、灵息、战力以及选择/取消按钮；选中态同时使用描边、底色和
“取消/槽位编号”文字，不只依赖颜色。系统返回键先关闭选人面板，再关闭入山整备面板。

点击“启程”打开第二层地图选择面板。每行显示地图编号、名称、所需灵息、每步灵粮和
最低携带灵粮；点击已解锁项目即执行最终校验并传送，不再增加一层确认。系统返回键先
关闭地图选择面板。任何修士灵息不足、灵粮不足、负重超限、阵亡或跨队重复都必须用
文字说明具体原因。

## 6.3 美术制作索引

入山整备、编辑队伍和地图选择的素材表、参考截图、GPT Image 提示词、营地/灵圃现有
资源复用关系、制作顺序和美术验收统一见
[《昆吾禁地》入山整备、编辑队伍与地图选择美术分册](../ArtAssets/04_入山整备编辑队伍与地图选择.md)。

本 PRD 只维护页面信息层级、交互、反馈与可访问性，不再复制美术制作提示词。

## 7. 战斗UI

- 四名队员头像与生命。
- 行动条。
- 三技能状态。
- 增益/减益。
- 敌人生命和关键状态。
- 嘲讽锁定。
- 1×/2×。
- 暂停。

美术站位只用于排版，不显示前后排。

## 8. 新手引导

目标：前10分钟完成第一次入山。

| 时间 | 引导 |
|---|---|
| 0–2分钟 | 营地资源和灵圃 |
| 2–5分钟 | 招募/领取固定队 |
| 5–8分钟 | 编队、装备、三技能 |
| 8–10分钟 | 灵粮、负重、入山 |
| 10–15分钟 | 逐格移动、迷雾、固定敌人 |
| 15–20分钟 | 属性检定和返回选择 |
| 20–35分钟 | 打造、还魂、剑修一转 |
| 35–45分钟 | Boss和地图2预览 |

引导原则：

- 先操作后解释。
- 一次只要求一个动作。
- 可重看。
- P2之后允许跳过已完成的新手引导。

## 9. 可访问性

- 关闭震屏。
- 降低闪光。
- 字号档位。
- 音量分组。
- 颜色辅助图标。
- 自动战斗速度可选。
- 键盘Escape/浏览器返回一致。

## 10. 功能需求

| ID | 需求 | 阶段 |
|---|---|---|
| P0-UX-001 | 竖屏、安全区、基础主题 | P0 |
| P1-UX-002 | 灰盒大厅、地图、战斗 | P1 |
| P2-UX-003 | 完整新手引导 | P2 |
| P2-UX-004 | 角色、转职、检定和结算页面 | P2 |
| P3A-UX-005 | 地图2和三类Boss标记 | P3A |
| P3B-UX-006 | 地图网络、阵营、洗髓 | P3B |
| P3C-UX-007 | 终局和结局 | P3C |
| P4-UX-008 | 辅助设置和可用性修订 | P4 |

## 11. 验收

- 新测试者三次点击内从大厅进入入山整备。
- 10分钟内第一次入山。
- 能说出灵粮和负重的区别。
- 能理解属性检定失败结果。
- 能识别三种Boss标记。
- 手机浏览器无被地址栏遮挡的关键按钮。
- 鼠标和触控均完成全流程。

## 12. 不做

- React UI。
- 横屏专用布局。
- 拖拽式大厅建造。
- 复杂手势作为唯一操作。
- 强制观看长教程。
