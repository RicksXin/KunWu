# 5. 灵圃生产弹窗

版本：0.1\
日期：2026-08-01\
状态：美术制作唯一事实源分册\
上级索引：[《昆吾禁地》美术素材制作总清单](README.md)


本节参考用户提供的《地下城堡 2》庄园生产面板截图，只借鉴“大厅上方打开纵向
生产面板、资源行右侧提供储量升级、操作后叠加二次弹窗”的信息结构，不复制其
双龙边框、银色浮雕、资源图标、升级按钮造型、工匠招募按钮造型、二次弹窗边框、
双倍收益、货币栏或其他具体视觉。

```text
大厅（保持原位置并变暗）
└─ 灵圃生产弹窗
   ├─ 标题“灵圃”
   ├─ 灵粮岗位：图标、库存/上限、净产量、升级、- / x/y / +
   ├─ 灵木岗位：图标、库存/上限、当前产量、升级、- / x/y / +
   ├─ 玄铁岗位：图标、库存/上限、当前产量、升级、- / x/y / +
   ├─ 任一资源升级：灵木消耗 + 当前上限 → 新上限 + 升级 / 取消
   └─ 结算倒计时 + 30 秒周期条 + 压在面板背景下边缘的杂役招募 / 关闭
      └─ 杂役招募：灵粮消耗 + 杂役 +5 + 招募 / 取消
```

P2 开放灵晶和庚精时启用 Prefab 中预留的两个资源行节点，不新增页面，也不改变为
滚动列表；尚未开放的资源仍不显示对应行。

## 5.1 精简后的素材清单

UI 使用 `375×817` 玩家可见窗口作为设计基准，PNG 按 `@3x` 交付。灵圃专属
素材进入 `assets/bundles/camp/ui/ling_pu/`；两张通用按钮进入 `assets/bundles/camp/ui/common/`；
共享资源图标进入 `camp/ui/top/`。
新图片必须由 Cocos Creator 导入并生成 `.meta`。

| 类型   | 素材               | 数量 |     逻辑尺寸 |          最终 PNG 尺寸 | 文件名                                            |
| ---- | ---------------- | -: | -------: | -----------------: | ---------------------------------------------- |
| 弹窗   | 生产弹窗九宫格边框        |  1 |  `96×96` | `288×288 px (@3x)` | `ui_ling_pu_panel_frame.png`                   |
| 行底板  | 资源岗位共用九宫格底板      |  1 |  `64×24` |  `192×72 px (@3x)` | `ui_ling_pu_resource_row.png`                  |
| 小按钮  | 增加/减少按钮（含符号）     |  2 |  `40×40` | `120×120 px (@3x)` | `icon_action_plus.png`、`icon_action_minus.png` |
| 通用按钮 | 资源行内紧凑操作普通态      |  1 |  `72×28` |       `396×102 px` | `ui_common_button_inline_normal.png`          |
| 通用按钮 | 面板底部/确认弹窗操作普通态（待重做） |  1 | `132×44` | 现版 `794×176 px`；重做交付 `396×132 px (@3x)` | `ui_common_button_footer_normal.png`          |
| 周期   | 倒计时条底/填充         |  2 | `160×10` |  `480×30 px (@3x)` | `ui_production_progress_{track,fill}.png`      |

灵圃当前直接依赖的 8 张图片均已放入运行时目录并由 Cocos Creator 生成 `.meta`。
两张全局通用普通态 PNG 位于 `assets/bundles/camp/ui/common/`：行内按钮已生成；
底部按钮现版因两侧外扩、底部基座和阶梯形外轮廓被退回，只能暂作交互占位，不得作为最终视觉或新素材参考。
`ui_ling_pu_action_button_normal.png` 已退出灵圃 Prefab 与 Presenter 的资源引用，
暂时保留文件，待用户完成视觉验收后再决定是否删除。

两张新按钮只交付普通态：按下使用运行时 `100ms` 缩放回弹，悬停与禁用由
Cocos `Button` Tint/透明度表达，不制作额外三态 PNG。灵粮、灵木、玄铁、灵晶和庚精
直接复用大厅顶部 `72×72` 运行时资源图标；五张图标均已整理至
`assets/bundles/camp/ui/top/`。灵圃 P1 当前的按钮美术缺口为“重做通用底部按钮”。

以下内容由 Cocos 节点、Label 和代码完成，不需要美术图片：

- 全屏半透明黑色遮罩、分隔线、滚动区域和按钮热区。
- 标题、库存/最大储量、岗位 `x/y`、当前产量和倒计时数字。
- 灵粮/灵木不足、满仓、满级、停工原因和恢复条件；使用文字与行底板 Tint 表达。
- 周期结算时的资源增减数字动画。
- 杂役招募二次弹窗复用 `ui_ling_pu_panel_frame.png`、灵粮图标和通用底部按钮，
  只需新建一层遮罩与布局节点，不增加专用 PNG。
- 资源行右上角的“升级”使用通用行内按钮；储量升级确认弹窗底部的
  “升级/取消”使用通用底部按钮。按钮分类由使用位置决定，不由文案决定。

明确删除此前规划的灵圃横幅、室内背景、杂役头像、返回/信息图标、生产状态图标、
专用警告面板和周期时钟图标。本轮不制作产量升级、整座建筑升级特效、双倍收益、
广告按钮或离线收益宝箱。资源储量升级与杂役招募都复用通用弹窗，并按按钮的使用位置选择行内或底部样式；参考图中
的升级、工匠招募与二次弹窗具体造型均不复刻。

## 5.2 需要提供的截图

#### 5.2.1 主参考截图

| 截图                               | 尺寸与本地路径                                                                               | 只参考                                                                   |
| -------------------------------- | ------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `微信图片_20260731171409_38_209.jpg` | `1256×2808`；`/Users/zhangxiaoen/Desktop/个人资料/referPng/微信图片_20260731171409_38_209.jpg` | 弹窗覆盖大厅、空闲人数、资源纵向列表、每行库存/上限、升级与加减分配、底部周期条和两枚主按钮                        |
| `升级建筑确认弹窗.jpg`                   | `1256×2808`；`/Users/zhangxiaoen/Desktop/个人资料/referPng/升级建筑确认弹窗.jpg`                   | 主弹窗上方再开二次弹窗、进一步压暗下层、灵木消耗、升级后最大储量和底部“升级/取消”双按钮层级；招募弹窗沿用同一层级但按钮为“招募/取消” |

两张截图都是第三方游戏内部设计参考，不得进入运行时 Bundle、公开构建或对外
素材包。

禁止参考或复制：双龙/双兽边框、银色金属浮雕、资源图标画法、“升级”按钮、工匠
招募按钮造型、二次弹窗边框、双倍收益、广告图标、字体、数值、具体间距和底部导航。

#### 5.2.2 项目截图

制作和后续回归时可归档以下五张本项目截图。五张使用同一 `375×817` 布局，只改变
弹窗层级、目标资源和资源是否足够；本轮已完成浏览器预览验收，但截图文件尚未归档：

| 截图                                                              | 必须显示                                                                                           | 状态        |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------- |
| `assets/refer/ling_pu_panel_layout_375x817.png`                 | 大厅停在灵圃位置并变暗；P1 弹窗只显示灵粮、灵木、玄铁三条资源行，包含库存/上限、当前产量、升级按钮和 3/2/1 分配；不显示灵晶、庚精占位；底部显示结算倒计时、周期条、杂役招募和关闭 | 待用户预览并归档  |
| `assets/refer/ling_pu_recruit_confirm_375x817.png`              | 二次弹窗显示灵粮图标、配置消耗、当前灵粮、“杂役 +5”、可用的招募和取消按钮                                                        | 预览已验收，待归档 |
| `assets/refer/ling_pu_recruit_insufficient_375x817.png`         | 与上一张布局一致，但当前灵粮小于配置消耗，显示“灵粮不足”，招募禁用、取消可用                                                        | 预览已验收，待归档 |
| `assets/refer/ling_pu_storage_upgrade_375x817.png`              | 从灵粮行打开升级弹窗；显示灵木图标、配置消耗、当前灵木、灵粮当前上限 → 新上限，以及可用的升级/取消按钮                                          | 逻辑已测，待归档  |
| `assets/refer/ling_pu_storage_upgrade_insufficient_375x817.png` | 与上一张布局一致，但当前灵木小于配置消耗，显示“灵木不足”，升级禁用、取消可用                                                        | 预览已验收，待归档 |

截图必须来自 Cocos 预览的 `375×817` 可见区域，不包含浏览器地址栏、开发者工具
或桌面。使用实际 Ark Pixel 字体和最终节点布局；美术尚未完成时可使用 Kenney
面板、纯色节点以及文字 `-`/`+` 占位。

生成时同时上传：

1. 用户提供的两张参考截图，只用于信息层级和弹窗嵌套关系。
2. 对应层级的本项目灰盒截图，用于本项目尺寸和布局。
3. `assets/bundles/camp/env_camp_panorama_bg.png`，只用于匹配营地色盘。
4. 加减按钮、通用行内按钮和通用底部按钮都只生成普通态；按下/禁用状态由运行时代码处理。

重做通用底部按钮时，不得把现有 `ui_common_button_footer_normal.png` 作为正向风格图上传。
如需让 GPT Image 理解退回原因，只能将它标注为“反例：禁止复制阶梯形外轮廓、外扩侧翼和底部基座”；
正向风格参考使用面板边框、资源行底板和已生成的通用行内按钮。

## 5.3 共用输出要求与提示词

- 每次只生成一张可拆分资产，不生成完整页面后再切图。
- 每张素材单独输出真实 Alpha 透明 PNG；面板和行底板必须标明九宫格安全拉伸区。
- 按钮只交付普通态 PNG；按下时只缩放可见子节点，用 `40ms` 缩小到 `94%`、
  `60ms` 回到原大小，按钮根节点和热区不缩放；悬停和禁用状态由 Cocos `Button`
  Tint/透明度表达。
- GPT Image 无法直接输出精确尺寸时，可先生成高清母版，再裁切、补透明边和缩放；
  最终尺寸必须与 5.1 表格一致。
- 运行时使用 Nearest 过滤和整数倍缩放；按钮图片放入至少 `48×48dp` 的节点内。

每条具体提示词前添加：

```text
为原创竖屏2D像素修仙游戏《昆吾禁地》制作灵圃生产弹窗的单个独立UI资产。参考的第三方游戏截图只用于理解“大厅上方弹出纵向生产面板、资源按行排列、每行显示库存上限并可升级和加减分配、操作时叠加二次弹窗、底部显示生产周期”的信息结构，不得复制其双龙边框、银色浮雕、图标、按钮、字体或具体构图。

整体视觉延续《昆吾禁地》山外营地：低饱和黛青、暖褐、深木、旧青铜、黑玉、灰绿和少量朱砂；2D pixel art，清晰像素簇，硬边缘，无抗锯齿，小尺寸清楚。中国修仙题材，古拙、克制、实用，不做华丽宫殿式装饰。

只输出下文指定的单个UI资产，透明背景，主体居中，不展示手机界面、完整游戏截图、使用示意图或素材拼版。
```

每条具体提示词末尾添加：

```text
最终交付PNG尺寸必须严格保持为具体提示词指定的@3x尺寸。不要任何汉字、英文字母、数字、资源数量、倒计时、红点、水印、Logo或棋盘格背景；不要双龙、双兽、骷髅、尖刺、银色欧式浮雕、哥特花边、金币、宝石、双倍收益、广告、充值、礼包或购买入口；不要复制参考截图的资源图标、升级/招募按钮造型、二次弹窗边框和其他按钮外形；不要平滑矢量、3D渲染、写实照片、油画、模糊渐变、科幻霓虹或日式神社风格。
```

`icon_action_plus.png` 是调岗增加符号，不是购买入口，具体提示词明确允许生成。

## 5.4 具体提示词

#### 5.4.1 生产弹窗九宫格边框

```text
制作灵圃生产弹窗共用的九宫格边框。使用深色风化木内板、旧青铜窄边、黑玉内沿和四角极简昆吾山纹卡扣；顶部中央保留平直、干净的标题区域，中央大面积低对比且可拉伸，用于承载当前阶段已开放的紧凑资源行和底部生产周期。轮廓稳重、实用、克制，不画建筑、梯田、水车、人物或资源。

基础逻辑尺寸必须严格为96×96，最终交付透明PNG尺寸必须严格为288×288 px（@3x）。四边和四角适合九宫格拉伸到约343×650的弹窗；不要双龙、双兽、尖刺、银色浮雕、文字或按钮。

导入九宫格时四边不可拉伸区统一设置为 `48 px`（对应 `16` 个逻辑像素），确保顶部
标题牌、上下横梁和四角山纹卡扣完整保留；不得使用旧的 `30 px` 边距，否则顶部和
底部装饰会进入中央拉伸区并产生压扁效果。当前 Prefab 仍处于旧 `1080×1920`
工程坐标，因此背景 Sprite 还需要按“目标显示宽度 / 288”做一次整体等比缩放，再由
九宫格只拉伸中央木板；不能只把 `288 px` 源图直接设置成约 `1029 px` 宽的 SLICED
节点，否则 Cocos 会保持边角原始像素高度，使头尾装饰看起来只有设计高度的约三分之一。
```

文件：`ui_ling_pu_panel_frame.png`

#### 5.4.2 资源岗位共用行底板

```text
制作灵圃资源岗位列表共用的九宫格行底板。使用低反光黛青黑玉内底和非常细的深木、暗青铜边；左侧预留资源图标，中部预留资源名称、当前库存/最大储量和当前产量文字，右侧预留储量升级按钮，下方预留岗位人数与加减按钮。底板低对比，不抢文字和资源图标，缩放后各行仍能清楚分隔。

基础逻辑尺寸必须严格为64×24，最终交付透明PNG尺寸必须严格为192×72 px（@3x），适合九宫格拉伸到约312×64。不要画图标、文字、数字、按钮或容量条；升级入口由独立通用行内按钮叠加。
```

文件：`ui_ling_pu_resource_row.png`

#### 5.4.3 增加与减少按钮

两张小按钮分别包含完整底板和增加/减少符号，不再拆分底板或制作按下/禁用 PNG：

| 按钮 | 具体提示词                                                                                                                   | 文件名                     |
| -- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 增加 | 制作灵圃分配杂役使用的方形增加按钮。深木外框、旧青铜包角、低反光黑玉内底，中央用两根短白玉筹条横竖交叠成清楚的增加符号；这是调岗按钮，不是购买、充值或货币入口。逻辑尺寸严格为40×40，最终透明PNG严格为120×120 px（@3x）。 | `icon_action_plus.png`  |
| 减少 | 制作与增加按钮同组的方形减少按钮。保持相同画布、外框、材质、体量和透明边界，中央只保留一根横向短白玉筹条。逻辑尺寸严格为40×40，最终透明PNG严格为120×120 px（@3x）。                            | `icon_action_minus.png` |

#### 5.4.4 通用行内按钮与通用底部按钮

这两张是全局通用组件，不是灵圃专属素材。生成时不使用 §5.3 中“灵圃生产弹窗”的
限定性开头，改用下面这段通用开头：

```text
为原创竖屏2D像素修仙游戏《昆吾禁地》制作单个全局通用横向按钮组件。按钮将在多个生产、编队、地图、确认与系统面板里复用，不得出现灵田、药草、农具、职业、地图或其他页面专属符号。

整体延续《昆吾禁地》低饱和黛青、暖褐、深木、旧青铜、黑玉和灰绿色盘；2D pixel art，硬边缘，清晰像素簇，无抗锯齿。只输出指定的单个按钮底图，真实 Alpha 透明背景，不含文字、图标或展示场景。
```

通用行内按钮：

```text
制作列表行、资源行和信息卡片内部使用的通用行内按钮普通态。它可承载“升级”“选择”“取消”“调息”等紧凑操作，文案全部由 Cocos Label 渲染。

造型是薄而平的黛青黑玉签牌：只使用一层极细旧青铜或灰绿描边，四角做克制的小斜切，内底低反光、中央干净。只允许一条极弱的上沿亮线表示可点击，不要厚重的立体台阶、外伸托架、左右支柱、脚座、大块灰白玉、强高光或浮雕。它在信息行中的视觉权重必须低于行文字和资源图标。

九宫格源图逻辑尺寸必须严格为72×28，最终交付透明PNG尺寸必须严格为216×84 px（@3x）。四角、斜切和描边全部放在左右各约10个逻辑像素、上下各约6个逻辑像素的不可拉伸区，中心保持低纹理，可横向拉伸至约56–120×28。可见按钮可小于48dp高，但运行时必须放入至少48dp高的独立交互节点。不要文字、图标、对勾、价格、资源、宝石或页面专属纹样。
```

文件：`ui_common_button_inline_normal.png`

通用底部按钮：

```text
制作面板底部固定操作区与二次确认弹窗使用的通用底部按钮普通态。它可承载“招募”“关闭”“升级”“取消”“完成”“返回”“启程”“离开”等面板级操作，文案全部由 Cocos Label 渲染。同一底图同时支持主操作和次操作，两者只通过运行时 Tint、Label 颜色和明度区分，不再制作第二张近似底图。

外轮廓必须是一个标准、连续、低调的横向长方形 UI 按钮：上边和下边是两条连续平直的水平线，左右边是等高的垂直线；四角最多只允许 `1–2` 个逻辑像素的单次小斜切，不得出现连续多层退让。整个按钮必须能被一个规整长方形紧密包住，边框、高光、暗角和材质纹理全部放在这个长方形内部。

材质使用低反光深黛玉或暗木内底，外围只有一层细旧青铜描边和一条极弱的上沿亮线。与行内按钮的区别来自更大的高度、更宽的文字区和稍暖的内底，不来自异形外轮廓或建筑式装饰。造型必须明确读作现代游戏 UI 中的平面按钮，不能读作木制牌匾、石制牌坊、建筑横梁或面板基座。

严格禁止：阶梯形、台阶形、多层退台、梯形外扩、左右扩展翼、侧边柱、突出端盖、下方基座、底座、脚座、托架、横梁、牌匾、牌坊、城门、建筑构件感；禁止任何装饰突破长方形外轮廓，禁止用透明缺口做出层层缩进的边缘。

在 `375×817` UI 基准中的逻辑显示尺寸必须严格为 `132×44`，重做后最终交付透明 PNG 尺寸必须严格为 `396×132 px (@3x)`。使用九宫格横向拉伸时，只允许中央内底变宽，四角、描边和按钮高度完全不变。不要文字、图标、对勾、价格、资源、宝石、奖励光芒或页面专属纹样。
```

文件：`ui_common_button_footer_normal.png`

两张文件输出至 `assets/bundles/camp/ui/common/`。按下、悬停和禁用态由 Cocos
Tween/Tint 生成，不需要额外提示词或 PNG。

#### 5.4.5 资源图标

灵圃直接复用大厅顶部 HUD 的资源图标与提示词，见
[营地 HUD 分册 §4.4 五种顶部资源图标](02_营地HUD.md#44-顶部-ui-提示词)。
当前灵粮、灵木、玄铁运行时图标均已存在并由 Cocos 导入；灵圃直接复用，不再生成
另一套图标。

#### 5.4.6 生产周期条

进度条底：

```text
制作灵圃30秒生产周期的横向像素进度条底。深木与旧青铜组成细长凹槽，内部为低反光黑玉，左右端对称，整体克制。逻辑尺寸严格为160×10，最终透明PNG严格为480×30 px（@3x）。不要填充、图标、刻度、数字或文字。
```

文件：`ui_production_progress_track.png`

进度条填充：

```text
制作叠加在生产周期条底上的独立填充条。逻辑尺寸严格为160×10，最终透明PNG严格为480×30 px（@3x）；填充是一条清晰的暗青绿至暖金像素灵液，左端完整、右端平直，运行时通过裁切改变长度。不要外框、数字、文字、固定百分比或大面积柔光。
```

文件：`ui_production_progress_fill.png`

## 5.5 制作顺序与验收

1. 先完成灵圃主弹窗、杂役招募弹窗和储量升级弹窗灰盒，截取 §5.2.2 的五张项目截图。
2. 先生成弹窗边框和资源行，在灰盒截图上合成检查 P1 三行无重叠，且库存/上限、升级和分配信息清楚。
3. 保留已通过方向确认的加减按钮和通用行内按钮，按新提示词重做通用底部按钮；重点验收连续矩形外轮廓与运行时状态。
4. 复用或补齐资源图标，最后生成周期条。
5. 所有素材导入 Cocos 后设置 Nearest、正确九宫格与表格中的逻辑尺寸。

验收：

- 点击灵圃后只出现一个纵向生产弹窗，大厅在后方变暗且不发生场景切换。
- P1 主弹窗只显示灵粮、灵木、玄铁三条可操作资源行；未开放的灵晶、庚精不显示，
  且没有产量升级、双倍收益、广告或付费入口。
- 玩家能在 3 秒内看到各岗位 `x/y`、库存/上限、当前产量、升级和 `-`/`+`。
- 灵粮行产量随灵粮岗人数增加，并随灵木/玄铁岗位维护实时减少；页面不显示储量等级、
  “下周期”汇总或“全部岗位可正常生产”文案。
- 主弹窗底部只有“杂役招募/关闭”，不存在“确认分配”按钮。
- 招募与储量升级弹窗复用主面板边框、对应资源图标和通用底部按钮；没有新增专用边框或资源图标。
- 灵粮不足时二次弹窗的招募按钮禁用，取消按钮仍可辨识且可用。
- 灵木不足时储量升级弹窗的升级按钮禁用；灵木充足时明确显示升级后的最大储量。
- 灵粮、灵木、玄铁与大厅顶部使用相同图标文件。
- 资源行内的“升级”只使用通用行内按钮，轮廓薄、对比低，不再出现旧按钮的厚木桩和灰白玉牌。
- 主弹窗和确认弹窗底部只使用通用底部按钮；必须是连续规整的横向长方形，不得有阶梯形、外扩侧翼、突出端盖或底部基座。
- 弹窗、资源行和按钮缩放后像素清楚，九宫格没有拉伸破损。
- 按钮按下时在 `100ms` 内完成 `94%` 缩小回弹，不改变热区；悬停和禁用 Tint 能清楚区分可用状态。
- 灵粮不足与停工通过文字说明，不只依赖颜色。
- 所有文案和数值由 Label 渲染，PNG 中没有烘焙文字或数字。
- 美术只借鉴参考截图的信息层级，没有复制双龙边框、银色浮雕、图标或按钮。
- 所有按钮节点至少 `48×48dp`，在目标分辨率和安全区内无遮挡。
